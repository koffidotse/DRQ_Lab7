import React from 'react';

//exporting class to extend to react component
export class Header extends React.Component {
    render() {
        return (
            <div>
                <h1>This is the header component.</h1>
            </div>
        );
    }
}